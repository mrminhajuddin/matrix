matrix = [[1,0,0],[0,1,0],[0,0,1]]
flag = True

for i in range(len(matrix)):
    for j in range(len(matrix)):
        if (i == j and matrix[i][j] != 1) or (i != j and matrix[i][j] != 0):
            flag = False

print("Identity Matrix" if flag else "Not Identity")
